<!doctype html>
<html lang="pt-BR">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Vibe Sensual • Catálogo</title>

  <link rel="stylesheet" href="/css/app.css" />
  <style>
    :root{
      --bg:#07040d;
      --card: rgba(255,255,255,.06);
      --stroke: rgba(255,255,255,.12);
      --txt: rgba(255,255,255,.92);
      --muted: rgba(255,255,255,.70);
      --pink:#ff4fd8;
      --purple:#8b5bff;
      --glow: 0 0 22px rgba(255,79,216,.20), 0 0 28px rgba(139,91,255,.18);
    }
    body{ background: radial-gradient(900px 650px at 50% 20%, rgba(139,91,255,.18), transparent 60%), var(--bg); color: var(--txt); }
    .wrap{ max-width: 1180px; margin: 0 auto; padding: 26px 16px 50px; }

    .top{ display:flex; align-items:flex-start; justify-content:space-between; gap:12px; flex-wrap:wrap; margin-bottom: 14px; }
    .brand{ padding: 10px 12px; border-radius: 16px; background: rgba(255,255,255,.04); border: 1px solid var(--stroke); box-shadow: var(--glow); }
    h1{ margin:0; font-size: 1.25rem; }
    p{ margin:6px 0 0; color: var(--muted); }

    .panel{ background: var(--card); border: 1px solid var(--stroke); border-radius: 18px; padding: 14px; box-shadow: 0 18px 40px rgba(0,0,0,.35); }

    .filters{ display:flex; gap:10px; align-items:center; flex-wrap:wrap; margin-top: 10px; }
    input, select{
      padding: 11px 12px; border-radius: 14px;
      background: rgba(255,255,255,.07);
      border: 1px solid var(--stroke);
      color: var(--txt);
      outline: none;
      min-width: 240px;
    }
    input:focus, select:focus{ box-shadow: var(--glow); border-color: rgba(255,79,216,.35); }

    .mini{
      padding: 10px 12px; border-radius: 14px; cursor:pointer;
      border: 1px solid rgba(255,255,255,.12);
      background: rgba(255,255,255,.06);
      color: var(--txt);
      text-decoration:none;
      display:inline-flex;
      align-items:center;
      gap:8px;
    }

    .grid{
      margin-top: 14px;
      display:grid;
      grid-template-columns: repeat(12, 1fr);
      gap: 14px;
    }

    .card{
      grid-column: span 3;
      background: rgba(255,255,255,.06);
      border: 1px solid rgba(255,255,255,.12);
      border-radius: 18px;
      overflow:hidden;
      box-shadow: 0 18px 40px rgba(0,0,0,.25);
      display:flex;
      flex-direction:column;
      min-height: 380px;
    }

    .imgbox{
      background: rgba(0,0,0,.22);
      border-bottom: 1px solid rgba(255,255,255,.10);
      aspect-ratio: 1 / 1;
      display:flex;
      align-items:center;
      justify-content:center;
      overflow:hidden;
    }
    .imgbox img{
      width:100%;
      height:100%;
      object-fit: cover;
      display:block;
    }
    .ph{
      width:100%;
      height:100%;
      display:flex;
      align-items:center;
      justify-content:center;
      color: rgba(255,255,255,.55);
      font-weight: 800;
    }

    .content{ padding: 12px 12px 14px; display:flex; flex-direction:column; gap:8px; flex:1; }
    .title{ margin:0; font-size: .98rem; font-weight: 900; line-height: 1.25; }
    .muted{ color: var(--muted); font-size: .86rem; }

    .prices{ margin-top:auto; }
    .de{ color: rgba(255,255,255,.55); text-decoration: line-through; font-size:.85rem; }
    .por{ font-size: 1.05rem; font-weight: 900; margin-top: 2px; }

    .btn{
      margin-top: 10px;
      width:100%;
      border:none; cursor:pointer;
      border-radius: 14px;
      padding: 11px 12px;
      color: rgba(255,255,255,.95);
      background: linear-gradient(90deg, rgba(255,79,216,.95), rgba(139,91,255,.92));
      box-shadow: var(--glow);
      font-weight: 900;
      text-decoration:none;
      display:inline-flex;
      justify-content:center;
      align-items:center;
      gap:8px;
    }

    @media (max-width: 1100px){ .card{ grid-column: span 4; } }
    @media (max-width: 820px){ .card{ grid-column: span 6; } }
    @media (max-width: 520px){
      .card{ grid-column: span 12; }
      input, select{ width: 100%; min-width: 0; }
      .mini{ width: 100%; justify-content:center; }
    }
  </style>
</head>
<body>
  <div class="wrap">
    <div class="top">
      <div class="brand">
        <h1>Catálogo • Vibe Sensual</h1>
        <p>Escolha um produto e veja os detalhes 💖</p>
      </div>

      <div class="brand">
        <div class="muted">Canal de preço: <b><%= canal %></b></div>
      </div>
    </div>

    <div class="panel">
      <form class="filters" method="get" action="/catalogo">
        <input name="q" value="<%= q || '' %>" placeholder="Buscar por nome ou SKU" />

        <select name="categoryId">
          <option value="">Todas categorias</option>
          <% (categories || []).forEach(c => { %>
            <option value="<%= c.id %>" <%= String(categoryId||'') === String(c.id) ? 'selected' : '' %>><%= c.nome %></option>
          <% }) %>
        </select>

        <input type="hidden" name="canal" value="<%= canal %>">

        <button class="mini" type="submit">Filtrar</button>
        <a class="mini" href="/catalogo" style="justify-content:center;">Limpar</a>
      </form>
    </div>

    <div class="grid">
      <% (products || []).forEach(p => { %>
        <div class="card">
          <div class="imgbox">
            <% if (p.cover_url) { %>
              <img src="<%= p.cover_url %>" alt="<%= p.nome %>">
            <% } else { %>
              <div class="ph">Sem imagem</div>
            <% } %>
          </div>

          <div class="content">
            <div class="title"><%= p.nome %></div>
            <div class="muted">SKU: <b><%= p.sku %></b></div>

            <div class="prices">
              <%
                function fmtBRL(v){
                  try {
                    const n = Number(v);
                    if (!isFinite(n)) return null;
                    return n.toLocaleString("pt-BR", { style: "currency", currency: "BRL" });
                  } catch (e) { return null; }
                }
                const por = p.preco_venda == null ? null : fmtBRL(p.preco_venda);
                const de = p.preco_de == null ? null : fmtBRL(p.preco_de);

                const showDe = !!de && !!por && (Number(p.preco_de) > Number(p.preco_venda));
              %>

              <% if (showDe) { %>
                <div class="de">De: <%= de %></div>
              <% } %>

              <div class="por">
                <%= por ? por : "Consulte" %>
              </div>
            </div>

            <a class="btn" href="/catalogo/produto/<%= p.id %>?canal=<%= canal %>">
              Selecionar
            </a>
          </div>
        </div>
      <% }) %>

      <% if (!products || products.length === 0) { %>
        <div class="panel" style="grid-column: span 12; color: var(--muted);">
          Nenhum produto publicado encontrado.
        </div>
      <% } %>
    </div>
  </div>
</body>
</html>
