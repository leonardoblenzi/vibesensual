<!doctype html>
<html lang="pt-BR">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Admin • Login</title>

  <link rel="stylesheet" href="/css/app.css" />
  <style>
    :root{
      --bg:#07040d;
      --card: rgba(255,255,255,.06);
      --stroke: rgba(255,255,255,.12);
      --txt: rgba(255,255,255,.92);
      --muted: rgba(255,255,255,.70);
      --pink:#ff4fd8;
      --purple:#8b5bff;
      --glow: 0 0 22px rgba(255,79,216,.20), 0 0 28px rgba(139,91,255,.18);
    }
    body{ background: radial-gradient(900px 650px at 50% 20%, rgba(139,91,255,.18), transparent 60%), var(--bg); color: var(--txt); }
    .wrap{ max-width: 980px; margin: 0 auto; padding: 30px 16px; }
    .box{
      max-width: 420px; margin: 10vh auto 0;
      border-radius: 20px;
      background: var(--card);
      border: 1px solid var(--stroke);
      box-shadow: 0 18px 44px rgba(0,0,0,.45), var(--glow);
      padding: 18px;
    }
    h1{ margin: 0 0 6px; font-size: 1.25rem; }
    p{ margin: 0 0 14px; color: var(--muted); }
    .row{ display:flex; flex-direction:column; gap:8px; margin-top: 10px; }
    label{ font-size: .88rem; color: var(--muted); }
    input{
      padding: 12px 12px;
      border-radius: 14px;
      background: rgba(255,255,255,.07);
      border: 1px solid var(--stroke);
      color: var(--txt);
      outline: none;
    }
    input:focus{ box-shadow: var(--glow); border-color: rgba(255,79,216,.35); }
    .btn{
      width: 100%;
      margin-top: 12px;
      border: none;
      cursor: pointer;
      border-radius: 14px;
      padding: 11px 12px;
      color: rgba(255,255,255,.95);
      background: linear-gradient(90deg, rgba(255,79,216,.95), rgba(139,91,255,.92));
      box-shadow: var(--glow);
      font-weight: 900;
    }
    .err{
      margin-top: 10px;
      color: rgba(255,255,255,.92);
      background: rgba(255,79,216,.16);
      border: 1px solid rgba(255,79,216,.25);
      padding: 10px 12px;
      border-radius: 14px;
    }
    .hint{ margin-top: 12px; font-size: .86rem; color: var(--muted); }
  </style>
</head>
<body>
  <div class="wrap">
    <div class="box">
      <h1>Admin • Vibe Sensual</h1>
      <p>Entre para gerenciar produtos, estoque e precificação.</p>

      <form method="post" action="/admin/login">
        <div class="row">
          <label for="password">Senha do Admin</label>
          <input id="password" name="password" type="password" placeholder="Digite a senha" required />
        </div>

        <button class="btn" type="submit">Entrar</button>

        <% if (error) { %>
          <div class="err"><%= error %></div>
        <% } %>

        <div class="hint">
          Dica: configure <b>ADMIN_PASSWORD</b> e <b>SESSION_SECRET</b nas env vars.
        </div>
      </form>
    </div>
  </div>
</body>
</html>
